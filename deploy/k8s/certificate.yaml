apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-manager-pomelo-dev
  namespace: <NAMESPACE>
spec:
  dnsNames:
  - <DOMAIN>
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod-dns
  renewBefore: 720h
  secretName: cert-manager-pomelo-dev
